/**
 * financialAnalytics.ts
 *
 * @deprecated Import directly from "@/src/services/finance/analytics" instead.
 *
 * Backward-compatible re-export barrel.
 * All logic has been moved to the `analytics/` sub-modules.
 * Existing imports of individual functions (e.g. `computeHealthScoreV2`)
 * continue to work without modification.
 */

export * from "./analytics";
